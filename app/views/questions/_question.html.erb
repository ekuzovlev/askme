<div class="question">
  <div class="question-content">
    <div class="question-text text-lead">
      <%= question.text %>
      <%= render partial: 'hashtags/hashtag', collection: question.hashtags %>
    </div>

    <% if question.answer.present? %>
      <div class="question-answer text-lead">
        <%= question.answer %>
      </div>
    <% end %>

    <div class="question-date">
      <%= l(question.created_at, format: :short) %>
        <span>| Спросил: </span>
      <% if author_name(question.author_id).nil? %>
        <%= 'Аноним' %>
      <% else %>
        <%= link_to author_name(question.author_id) , user_path(question.author_id) %>
      <% end %>
    </div>
  </div>

  <% if question.user == current_user %>
    <% edit_icon = question.answer.blank? ? 'reply' : 'pencil' %>

    <div class="question-actions">
      <%= link_to fa_icon(edit_icon), edit_question_path(question) %>
      <%= link_to fa_icon('trash-o'), question_path(question),
                  method: :delete, data: {confirm: 'Точно удалить вопрос?'} %>
    </div>
  <% end %>
</div>
